Flask==3.0.0
Werkzeug==3.0.1
Pillow==10.1.0
numpy==1.26.2

# Streamlit for testing/development interface
streamlit>=1.30.0

# HuggingFace Transformers for AI-generated image detection (needs 4.37+ for SigLIP support)
transformers>=4.40.0

# PyTorch CPU-only version (smaller, faster download)
# Upgraded to >= 2.6 for security fix (CVE-2025-32434) - required for CLIP model loading
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.6.0
torchvision>=0.21.0

# Optional additional ML libraries
opencv-python==4.8.1.78  # Required for video frame extraction

# CLIP for frame categorization
ftfy>=6.1.0  # Required by CLIP
regex>=2023.0.0  # Required by CLIP

# Face detection for cropping faces from frames
facenet-pytorch>=2.5.3  # Includes MTCNN face detector

# scikit-learn==1.3.2
# onnxruntime==1.16.3
